import React from 'react';
import { useCalculator } from '../../contexts/CalculatorContext';
// Note: This is a placeholder component that will be removed
// We've removed tax calculations from the application

// Temporarily import the placeholder utility to satisfy IDE
import { calculateTax } from '../../utils/taxCalculator';

const TaxCalculator = () => {
  // This component has been deprecated and is no longer used
  return null;
};

export default TaxCalculator;
